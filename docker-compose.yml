services:
  insightface:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8002:8002"
    environment:
      - PYTHONUNBUFFERED=1
      - ENABLE_SWAGGER=true
    command: python3 -m uvicorn main:app --host 0.0.0.0 --port 8002 --reload
